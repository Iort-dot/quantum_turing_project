The goal is to transform a classical Turing machine into a Turing machine with 2 qubits. 

With a Hadamard gate and a NOT, we use the results and erorrs and if the errors < 0.1, the Turing machine continue to the Stop cell. 
